{
  "files": [
    "rollout.captured.jsonl",
    "rollout-content.captured.jsonl",
    "turn-aborted.captured.jsonl",
    "session-index.captured.jsonl",
    "rollout-item-completed.captured.jsonl"
  ],
  "isVerbatimCapturedOutput": true,
  "codexVersion": "codex-cli 0.148.0 for rollout.captured.jsonl, rollout-content.captured.jsonl, turn-aborted.captured.jsonl and session-index.captured.jsonl; codex-cli 0.153.4 for rollout-item-completed.captured.jsonl",
  "capturedOn": "2026-08-19, and 2026-08-22 for rollout-content.captured.jsonl, and 2026-09-09 for rollout-item-completed.captured.jsonl",
  "sha256": {
    "rollout.captured.jsonl": "550f40340290106c03b8b480995684003e58163c845ab59e8419a62bff3ac5a1",
    "rollout-content.captured.jsonl": "6758724cc4eb206ab2d347172fcc10e52930640fec372e563068b1eb63d6608b",
    "turn-aborted.captured.jsonl": "f437570cf0e8fc36a7966a8fa85276eb813b629ca22770c3ee246052a4dd9466",
    "session-index.captured.jsonl": "be592690cf76bcaf0080aebb3de32e1446f8478d1ccbb7d532411a93960f4986",
    "rollout-item-completed.captured.jsonl": "c9a22685d459de13d58fda83215f60993f2a6f1990184982fef38e6b37d49e07"
  },
  "capturedBy": [
    "A thread created over `codex app-server` under an isolated CODEX_HOME, then driven by a",
    "real `codex resume` TUI and by `codex exec resume --skip-git-repo-check`. The index file",
    "is that home's session_index.jsonl after three renames: one at creation, one from a",
    "second app-server connection, one typed as `/rename` into the TUI.",
    "turn-aborted.captured.jsonl is a single line lifted from local codex history — no probe",
    "produced an interrupted turn, and the record carries no prose.",
    "rollout-content.captured.jsonl is a second rollout, captured 2026-08-22 from three turns",
    "under a throwaway CODEX_HOME, and NOT filtered to event_msg: it keeps the response_item",
    "records the timeline mapper reads and rollout.captured.jsonl has none of. The plan's",
    "recipe for it — a bare `codex exec` under an isolated CODEX_HOME — misses the production",
    "shape twice over. An isolated home has no credentials, so auth.json was copied into it;",
    "and a thread CREATED by `codex exec` (session_meta source \"exec\") writes an",
    "item_completed event surface, where a thread created over `codex app-server` (source",
    "\"vscode\", which is what CodexAdapter.prepare does) writes user_message/agent_message —",
    "even for turns a different process drives later. So this was captured the way Flight Deck",
    "runs: thread/start, thread/name/set and archive/unarchive over a real `codex app-server`,",
    "then three turns driven by `codex exec resume --skip-git-repo-check` in a temp workdir.",
    "The third turn calls a throwaway stdio MCP server (an echo tool, written for the capture)",
    "to get an mcp_tool_call_end record. codex-cli 0.148.0 routes even that through its",
    "unified `exec` custom_tool_call, and no invocation on this machine could make it write a",
    "function_call at all — that shape is covered by CodexTimelineMapperTests from records in",
    "the 494-rollout local corpus instead of by this capture.",
    "",
    "Two lines were dropped from rollout-content.captured.jsonl AFTER it was first committed:",
    "the response_item/message with role \"developer\" and the world_state record. Both carried",
    "this machine's home directory and an inventory of the operator's private ~/.agents/skills",
    "with descriptions — six paths each — which no other file in this directory carries. The",
    "lesson worth keeping: codex reads ~/.agents/skills regardless of CODEX_HOME, so a",
    "throwaway home does NOT isolate everything, and a capture has to be scanned rather than",
    "assumed clean because it was taken under one. Neither record maps to a timeline row, so",
    "nothing was lost but one role \"developer\" example, which CodexTimelineMapperTests covers",
    "by hand. The <recommended_plugins> prompt-assembly line is a different record and is",
    "still here.",
    "",
    "What remains, deliberately: the temp workdir path (a throwaway /var/folders directory),",
    "the timezone America/Chicago from codex's own <environment_context>, OpenAI's",
    "base_instructions, and the public plugin and skills catalogues. Scanned for credentials",
    "and account identifiers; there are none.",
    "",
    "rollout-item-completed.captured.jsonl is codex-cli 0.153.4's `event_msg`/`item_completed`",
    "prose surface (Task 2 of the plan that added the mapper arm reading it), captured because",
    "every other file here predates codex-cli 0.151 and so cannot exercise that arm at all. Two",
    "trivial turns, run with `codex exec --skip-git-repo-check '<prompt>'` then `codex exec",
    "resume --last --skip-git-repo-check '<prompt>'`, in a throwaway workdir under $TMPDIR (not",
    "$HOME) with CODEX_HOME pointed at an isolated, empty home under the same $TMPDIR tree —",
    "auth.json copied in, exactly as rollout-content.captured.jsonl's recipe above. The first",
    "turn was 'Run the shell command: echo hi. Then reply with exactly the word: done'; the",
    "second, in the same thread, was 'Reply with exactly the word: finished'.",
    "",
    "A thread `codex exec` CREATES writes `session_meta` `originator: \"codex_exec\"` /",
    "`source: \"exec\"`, and that originator/source pair is what makes codex write the new",
    "`item_completed` envelope instead of the three-arm `user_message`/`agent_message`/",
    "`agent_reasoning` surface the other files here carry — confirmed by reading this capture's",
    "own session_meta line. TUI is not represented here: driving one needs a PTY, and the",
    "record shapes an exec thread and a codex-tui/cli thread write for `item_completed` are",
    "structurally identical on this machine (same record keys, same payload keys, same",
    "`content[].type` casing — `\"text\"` for UserMessage, `\"Text\"` for AgentMessage), so this",
    "capture proves the exec case byte-for-byte and the TUI case only by that structural",
    "identity, not directly. See `.superpowers/sdd/resilient-twirling-kernighan/",
    "item-completed-shapes.md` for the side-by-side of both.",
    "",
    "Two lines were dropped before this file was ever committed, both by the same rule",
    "rollout-content.captured.jsonl's capturedBy paragraph already states: codex reads",
    "`~/.agents/skills` regardless of CODEX_HOME, so the throwaway home here did not isolate",
    "it either. One `response_item`/`message` record with `role: \"developer\"` carried the",
    "`<skills_instructions>` block naming this machine's real home directory",
    "(`/Users/nate/.agents/skills`) and an inventory of the operator's private skills there,",
    "six entries with descriptions; the paired `world_state` record repeated the same skills",
    "catalogue and home path inside its `host_skills` field. Neither record maps to a timeline",
    "row — the mapper reads `event_msg`/`item_completed` and `response_item`/`function_call`,",
    "`custom_tool_call` and their `*_output` counterparts only — so nothing was lost from",
    "mapped coverage. What remains, deliberately: the two turns' `item_completed` records for",
    "both prompts and both replies, the `custom_tool_call`/`custom_tool_call_output` pair for",
    "the `echo hi` command (codex-cli 0.153.4 routes even a plain shell command through the",
    "unified `exec` custom tool, the same as 0.148.0 did), the `<recommended_plugins>` public",
    "plugin catalogue (kept for the same reason rollout-content.captured.jsonl keeps it — a",
    "different record from the two dropped above), the temp workdir path under $TMPDIR, and",
    "token/turn bookkeeping records the mapper does not read. Scanned by hand afterward for",
    "emails, API keys and account ids beyond the two dropped lines; there are none. No",
    "`Reasoning` item_completed record appears in this capture at all — this session ran with",
    "reasoning effort `none` — so the mapper's documented loss of `.thinking` for this surface",
    "is not even exercised by omission here; TimelineFixtureTests asserts the absence directly",
    "against records surveyed elsewhere instead (see the test's own comment)."
  ],
  "editingRule": [
    "Lines may be DROPPED, never EDITED. rollout.captured.jsonl is the source rollout filtered",
    "to `session_meta` and `event_msg` records. rollout-content.captured.jsonl is its own",
    "source rollout with two records dropped for privacy (see capturedBy) and the fourth and",
    "last turn dropped as a duplicate in shape of the first. In both, every surviving line is",
    "byte-for-byte what codex wrote. The sha256 map above is of these files as committed and",
    "TimelineFixtureTests asserts it, so a semantic edit that still parses as JSON is caught",
    "rather than only a truncation. rollout-item-completed.captured.jsonl follows the same",
    "rule: two records dropped for privacy before first commit (see capturedBy), every",
    "surviving line byte-for-byte what codex wrote.",
    "",
    "Unlike codex-app-server-v2.generated.json, this is not generated from a schema, because",
    "no schema for these formats exists — `codex app-server generate-json-schema` covers the",
    "protocol only. That makes these files the weakest ground truth in the suite: they record",
    "what codex DID on one day, not what it DECLARES. `CodexIntegrationTests` is what notices",
    "when that stops being true; run it with FLIGHT_DECK_CODEX_INTEGRATION=1.",
    "",
    "Every fixture in this directory MUST be named *.captured.* to signal that it is verbatim",
    "captured output, not authored. Tests must reference captures by their documented name; if",
    "a test cannot find its fixture, correct the lookup name in the test, never add an undocumented",
    "copy under another name. Undocumented copies hide the data source and can silently diverge from",
    "the original when concurrent sessions commit. This rule prevents that category of bug."
  ]
}
